spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/ttokcheong-debate-summary-test
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update